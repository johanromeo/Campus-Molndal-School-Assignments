public interface HandlersMethods { // Interface giving methods for creating a collection and inserting documents
                                  // for any database that implements this interface (HandlerClasses)
    void createCollection();
    void insertDoc();
}
